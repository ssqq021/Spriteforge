# Sprint 3 Hotfix：播放启用与抽帧规则

## 修正后的语义

时间轴上的以下按钮全部控制 `播放时启用该帧`：

- 全选：全部帧启用
- 全不选：全部帧禁用
- 隔 1 帧抽：启用第 1、3、5、7… 帧
- 隔 2 帧抽：启用第 1、4、7、10… 帧

帧卡片底部：

- “启用”复选框：控制播放时是否启用
- 右侧小复选框：只用于批量删除勾选

## 验收

1. 导入 10 帧。
2. 点击“全不选”，播放启用数应为 0。
3. 点击“全选”，播放启用数应为 10。
4. 点击“隔 1 帧抽”，应启用 1、3、5、7、9，共 5 帧。
5. 点击“隔 2 帧抽”，应启用 1、4、7、10，共 4 帧。
6. 播放时只播放启用帧。
