# Sprint 3：素材数据库、视频抽帧与批量帧管理

## 已实现

- IndexedDB 本地素材数据库
- 新导入图片不再以 Base64 存入 LocalStorage
- MP4 / WebM / 浏览器可解码 MOV 视频导入
- 自定义抽帧 FPS
- 最大抽帧数量限制
- 抽帧进度显示
- 视频帧来源时间记录
- 图片和视频帧统一进入动作时间轴
- 帧批量选择
- 全选、全不选
- 删除选中
- 清空当前动作
- 删除项目、人物和动作时同步清理素材 Blob
- 首页显示浏览器存储使用量
- 兼容 Sprint 2 已保存的 Base64 帧

## 视频抽帧实现

Sprint 3 使用浏览器原生：

- HTMLVideoElement 解码
- Canvas 绘制
- PNG Blob 编码
- IndexedDB 保存

全部在本机执行，不上传视频。

## 当前限制

- 浏览器能否读取 MOV 取决于视频编码格式。
- H.264 MP4 和 WebM 通常兼容性最好。
- 首版最多允许一次抽取 600 帧，避免浏览器内存失控。
- 视频抽帧期间不要关闭页面。
- 当前还没有裁剪、统一尺寸和角色位置对齐。

## 验收

1. 进入人物或动画页面。
2. 选择人物和动作。
3. 点击“视频抽帧”。
4. 选择一个短 MP4。
5. 设置 12 FPS、最多 240 帧。
6. 点击开始抽帧。
7. 确认进度数字变化。
8. 抽帧完成后确认时间轴出现 VIDEO 标记帧。
9. 播放动画。
10. 测试全选、全不选、删除选中和清空。
11. 刷新页面，确认视频帧仍存在。
12. 首页检查本地存储使用量。
13. 执行 `npm run build`。

## Sprint 4

- 单帧裁剪
- 统一画布尺寸
- 角色位置稳定与锚点
- 去黑底
- Alpha 修边
- 批量图像处理管线
