# 贡献与协作规范

## 分支

- `main`：稳定版本
- `develop`：日常集成
- `feature/*`：功能
- `fix/*`：缺陷
- `docs/*`：文档

## Commit 格式

```text
feat: 新增动作帧排序
fix: 修复透明边缘黑边
docs: 更新 Unity 导出协议
refactor: 重构项目存储模块
test: 增加 SpriteSheet 导出测试
```

## Definition of Done

一个任务完成必须满足：

1. 功能可操作
2. 错误状态有提示
3. 不覆盖原始文件
4. 关键逻辑有测试
5. 文档同步更新
6. 在 Windows Chrome 验证
