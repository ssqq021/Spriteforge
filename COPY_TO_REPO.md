# 合并到 Spriteforge 仓库

把本包内容复制到本地 `Spriteforge` 仓库根目录，然后执行：

```powershell
git checkout -b develop
git add .
git commit -m "feat: initialize Sprint 1 workspace"
git push -u origin develop
```

在 GitHub 创建 `develop → main` Pull Request，验收后再合并。
