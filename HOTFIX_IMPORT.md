# Sprint 2 Hotfix：导入按钮无响应

修复内容：

- 将程序触发的隐藏文件框改为浏览器原生 `label → input` 触发。
- 增加 `.png / .jpg / .jpeg / .webp` 文件扩展名过滤。
- 未选择动作时显示明确提示。
- 未选择动作时导入按钮显示禁用状态。

验收：

1. 进入人物或动画工作区。
2. 左侧选择或创建一个动作。
3. 点击“导入图片序列”。
4. Windows 文件选择窗口应立即弹出。
5. 多选 PNG、JPG 或 WebP 后确认。
